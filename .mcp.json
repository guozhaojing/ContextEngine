{
  "mcpServers": {
    "contextengine": {
      "command": "dotnet",
      "args": [
        "run",
        "--",
        "--mcp",
        "--repo",
        "${workspaceFolder}"
      ],
      "cwd": "${workspaceFolder}",
      "env": {}
    }
  }
}
